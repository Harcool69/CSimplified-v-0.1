#ifndef CSIMPLIFIED_UTILS_H
#define CSIMPLIFIED_UTILS_H

char *trim(char *s);
int starts_with(const char *s, const char *prefix);
char *strdup_safe(const char *s);
int is_number(const char *s);

#endif
